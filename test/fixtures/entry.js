import sprite from "virtual:svg-sprite";

// Reference every export so nothing is tree-shaken away.
export const href = sprite.href;
export const ids = sprite.ids;
export const source = sprite.source;
